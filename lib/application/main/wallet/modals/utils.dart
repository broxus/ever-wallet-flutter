import 'package:intl/intl.dart';

final transactionTimeFormat = DateFormat('dd.MM.yyyy, H:mm');

final transactionTextShortTimeFormat = DateFormat('d MMM, H:mm');
final transactionTextLongTimeFormat = DateFormat('d MMM yyyy, H:mm');
